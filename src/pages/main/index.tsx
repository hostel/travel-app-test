import React from 'react';

import { WrapList } from '@components/hotels/wrapList';

/**
 * Main page
 *
 * @returns {React.ReactElement} - element
 */
export const Main = (): React.ReactElement => {
  return <WrapList />;
};
